<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Főoldal</title>
	<script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
	<div class="flex">
		<%- include('./navbar.ejs'); %>
		<!-- Content-->
		<div class="w-4/5 bg-white-500 p-10">
			<table class="mt-20 table-auto rounded-t-md shadow w-full">
				<thead class="h-10 rounded-t-md ">
					<tr>
						<th></th>
						<th class="p-2 text-left">Név </th>
						<th class="p-2 text-left">Létrehozás dátuma </th>
						<th class="p-2 text-left">Tulajdonos </th>
					</tr>
				</thead>
				<tbody>
					<% for (let i = 0; i < folders.length; i++) { %>
						<tr class="bg-gray-400">
							<td class="p-2 text-left"><a href="/explorer/<%= folders[i][0] %>"><img src="folder.svg" class="h-6"></a></td>
							<td class="p-2 text-left"><%= folders[i][1] %></td>
							<td class="p-2 text-left"><%= new Date(folders[0][4]).toLocaleString('default', { year: 'numeric', month: 'long', day: '2-digit' }) %></td>
							<td class="p-2 text-left"><%= folders[i][3] %></td>
							<td></td>
						</tr>
					<% } %>

					<% for (let i = 0; i < files.length; i++) { %>
						<tr class="bg-gray-400">
							<td class="p-2 text-left "><a href="/file/<%= files[i][0] %>"><img src="file.svg" class="h-6"></a></td>
							<td class="p-2 text-left "><%= files[i][3] %></td>
							<td class="p-2 text-left "><%= new Date(files[0][5]).toLocaleString('default', { year: 'numeric', month: 'long', day: '2-digit' }) %></td>
							<td class="p-2 text-left "><%= files[i][2] %></td>
							<td class="text-left w-auto">
								<a href="/download/<%= files[i][0]%>">Letöltés</a>
							</td>
						</tr>
					<% } %>
				</tbody>
			</table>
		</div>
	</div>
</body>

</html>