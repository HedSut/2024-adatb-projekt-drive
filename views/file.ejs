<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= file[3] %></title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="flex">
        <%- include('./navbar.ejs'); %>
            <!-- Content-->
            <div class="w-4/5 bg-white-500 p-10">
                <p class="mb-5 font-bold">
                    <%= file[3] %>
                </p>
                <div class="flex">
                    <div class="w-1/2 flex flex-col p-5">
                        <% if (file[2] == username) { %>
                        <div>
                            <form class="flex flex-row" action="/changevisibility" method="post">
                                <select name="visibility"
                                    class="mr-2 appearance-none bg-white border border-gray-300 hover:border-gray-500 px-4 py-2 pr-8 rounded shadow leading-tight focus:outline-none focus:border-blue-500">
                                    <option value="private" selected>Privát</option>
                                    <option value="public">Publikus</option>
                                </select>
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <button type="submit" class="p-1 w-20 bg-gray-300 rounded-md">
                                    Mentés
                                </button>
                            </form>
                        </div>

                        <div class="mt-5">
                            <form class="flex flex-row mt-5" action="/addshare" method="post">
                                <input type="hidden" name="type" value="file">
                                <input type="hidden" name="currentFolder" value="<%= file[0] %>">
                                <input type="hidden" name="id" value="<%= file[0]%>">
                                <button type="submit" class="cursor-pointer font-bold mr-2 w-1/5 p-2 rounded-md shadow-md hover:bg-gray-300 transition-color duration-300">
                                    Megosztás
                                </button>
                                <input type="text" placeholder="Felhasználónév" name="user"
                                    class="shadow-md rounded-md w-4/5 mr-2 p-2 focus:outline-none focus:border focus:border-blue-500">
                            </form>
                        </div>
                        <% } %>

                        <% if(username) { %>
                        <div class="flex mt-5">
                            <form action="/ratefile" method="post">
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <input type="hidden" name="value" value="-1">
                                <button type="submit">
                                    <img src="/arrow.svg"
                                        class="w-10 p-2 rounded-md bg-gray-300 hover:bg-gray-500 duration-300">
                                </button>
                            </form>
                            <p class="font-bold p-2">
                                <%= rating %>
                            </p>
                            <form action="/ratefile" method="post">
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <input type="hidden" name="value" value="1">
                                <button type="submit">
                                    <img src="/arrow.svg"
                                        class="w-10 p-2 rotate-180 rounded-md bg-gray-300 hover:bg-gray-500 duration-300">
                                </button>
                            </form>
                        </div>

                        <% if(!isBookmark) { %>
                        <div class="mt-5">
                            <form class="flex flex-col" action="/addbookmark" method="post">
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <button type="submit" class="w-2/5 p-2 bg-gray-300 rounded-md">
                                    Könyvjelzőhöz adás
                                </button>
                            </form>
                        </div>
                        <% } %>

                        <% if(isBookmark) { %>
                        <div class="mt-5">
                            <form class="flex flex-col" action="/deletebookmark" method="post">
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <button type="submit" class="w-2/5 p-2 bg-gray-300 rounded-md">
                                    Könyvjelző eltávolítása
                                </button>
                            </form>
                        </div>
                        <% } %>
                        <div class="mt-5">
                            <form class="flex flex-col" action="/addcomment" method="post">
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <textarea placeholder="Hozzászólás ... " name="text"
                                    class="shadow-md rounded-md w-full mb-2 p-2 focus:outline-none focus:border focus:border-blue-500"></textarea>
                                <button type="submit" class="w-1/5 p-2 bg-gray-300 rounded-md">
                                    Közzététel
                                </button>
                            </form>
                        </div>

                        <% } %>
                        <a href="/download/<%= file[0]%>" class="w-1/5">
                            <div class="p-2 mt-2 rounded-md text-center bg-gray-300">
                                Letöltés
                            </div>
                        </a>
                    </div>
                </div>

                <% for(let i=0; i < comment.length; i++) { %>
                    <div class="w-full mt-5 shadow-md rounded-md p-5">
                        <div class="h-auto flex mb-5">
                            <img src="/defaultpfp.png" class="shadow-md h-20 rounded-full overflow-clip mr-5">
                            <div>
                                <p class="flex mt-1 font-bold">
                                    <%= comment[i][1] %> · <%=new Date(comment[i][4]).toLocaleString('default', {
                                            year: 'numeric' , month: 'long' , day: '2-digit' , hour: '2-digit' ,
                                            minute: '2-digit' , second: '2-digit' })%>
                                </p>
                                <p class="text-justify">
                                    <%= comment[i][3] %>
                                </p>
                            </div>
                        </div>
                        <% if(comment[i][1]==username) {%>
                            <form class="flex flex-col" action="/deletecomment" method="post">
                                <input type="hidden" name="commentid" value="<%= comment[i][0] %>">
                                <input type="hidden" name="fileid" value="<%= file[0] %>">
                                <button type="submit" class="w-1/5 p-2 bg-gray-300 rounded-md">
                                    Törlés
                                </button>
                            </form>
                            <% } %>


                    </div>


                    <% } %>
            </div>
    </div>
</body>

</html>